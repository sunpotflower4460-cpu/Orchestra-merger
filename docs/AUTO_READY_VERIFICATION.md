# Auto-ready Verification Marker

This file exists only to exercise the single-repo self-driving loop:

queued issue -> Copilot draft PR -> CI Check success
-> auto-ready (draft -> ready) -> auto-merge -> issue close.

This is a docs-only marker. It contains no executable logic.
